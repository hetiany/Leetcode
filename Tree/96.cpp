#include <vector>
#include <algorithm>
#include <stack>
#include <queue>
using namespace std;

 struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 };



//Unique Binary Search Trees, DP
class Solution {
public:
    int numTrees(int n) {
        
    }
};

int main(){

	return 0;
}